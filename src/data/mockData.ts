import { School, Meet, Athlete, TrackEvent } from '../types';

// Initialize empty arrays for all data types
export const schools: School[] = [];
export const trackEvents: TrackEvent[] = [];
export const meets: Meet[] = [];
export const athletes: Athlete[] = [];